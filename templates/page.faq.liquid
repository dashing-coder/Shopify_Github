{% include 'page-header', title: {{ page.title }} %}

<section class="about section">
    <div class="container">
      <div class="row">
        <div class="col-md-6">
        <h1>{{ page.title }}</h1>
        </div>
        <div class="col-md-6">
          {{ page.content }}
          {% section 'page-faq-template'  %}
        </div>
      </div>
    </div>
  </section>
